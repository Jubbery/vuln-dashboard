# Production builds load the committed sample: the full 270MB scan exceeds
# free-tier hosting limits. Same worker, tokenizer, and normalizer either way.
# Override for a self-hosted full copy:
#   VITE_DATA_URL=https://your-bucket/ui_demo.json npm run build
VITE_DATA_URL=/ui_demo.sample.json
